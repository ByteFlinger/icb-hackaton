'use strict';

const fs = require('fs');
const app  = require('../app');
const port = 8080;

// Setup HTTP server
const server = require('http').createServer(app);
server.listen(port, () => {
  console.log(`Listening on ${port} for HTTP`);
});